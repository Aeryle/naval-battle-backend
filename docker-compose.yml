version: '3.8'

services:
  database:
    image: 'postgres:latest'

    ports:
      - '5432:5432'

    environment:
      POSTGRES_USER: navalbattle
      POSTGRES_PASSWORD: navalbattle
      POSTGRES_DB: navalbattle
